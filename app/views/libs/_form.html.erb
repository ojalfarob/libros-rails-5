<%= form_with(model: lib, local: true) do |form| %>
  <% if lib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lib.errors.count, "error") %> prohibited this lib from being saved:</h2>

      <ul>
      <% lib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :lib_nombre %>
  </div>

  <div class="field">
    <%= form.label :descrip %>
    <%= form.text_area :descrip, id: :lib_descrip %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
