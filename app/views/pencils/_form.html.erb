<%= form_with(model: pencil, local: true) do |form| %>
  <% if pencil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pencil.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% pencil.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pencil_name %>
  </div>

  <div class="field">
    <%= form.label :context %>
    <%= form.text_area :context, id: :pencil_context %>
  </div>

  <div class="field">
    <%= form.label :num %>
    <%= form.number_field :num, id: :pencil_num %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
